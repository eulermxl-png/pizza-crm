# Copy to .env.local for development. Set the same keys in Vercel → Settings → Environment Variables.
# Docker Compose also reads a file named ".env" in this folder (if you create it) to substitute variables in docker-compose.yml.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
